name: amana
root: ~/work/amana
windows:
  - package:
      panes:
        - cd package && source venv/bin/activate.fish && vs
        - cd package && source venv/bin/activate.fish && vs
        - cd package && source venv/bin/activate.fish && vs
        - cd package && source venv/bin/activate.fish && vs
  - backend:
      panes:
        - cd backend && source api/venv/bin/activate.fish && vs
        - cd backend && source api/venv/bin/activate.fish && vs
        - cd backend && source api/venv/bin/activate.fish && vs
        - cd backend && source api/venv/bin/activate.fish && vs
  - backend-droplet:
      panes:
        - ssh root@161.35.172.25
        - ssh root@161.35.172.25
        - ssh root@161.35.172.25
        - ssh root@161.35.172.25

