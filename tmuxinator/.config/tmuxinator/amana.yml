name: amana
root: /media/tarik/Data/work/pk/amana/repos
windows:
  - console:
      layout: even-vertical
      panes:
        - clear
        - cd amana && source amana-env/bin/activate.fish && cd _ooad/notebooks && jupyter lab
  - repo:
      layout: even-vertical
      panes:
        - cd amana && source amana-env/bin/activate.fish && vs
  - database:
      layout: even-vertical
      panes:
        - docker  run --name oracle19c --network host -p 1521:1521 -p 5500:5500 -v /u01/oracle:/opt/oracle/oradata oracle/database:19.3.0-ee
        - dx oracle19c bash

