name: amana
root: ~/work/amana
windows:
  - package:
      panes:
        - cd package && source venv/bin/activate.fish && vs
        - cd package && source venv/bin/activate.fish && vs
        - cd package && source venv/bin/activate.fish && vs
        - cd package && source venv/bin/activate.fish && vs
  - backend:
      panes:
        - cd backend && vs
        - cd backend  
        - cd backend  
        - cd backend  
  - backend-droplet:
      panes:
        - ssh root@46.101.93.245
        - ssh root@46.101.93.245
        - ssh root@46.101.93.245
        - ssh root@46.101.93.245

