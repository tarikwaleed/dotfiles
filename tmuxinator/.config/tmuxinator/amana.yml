name: amana
root: /media/tarik/Data/work/pk/amana/repos/amana
windows:
  - console:
      layout: main-vertical
      panes:
        - clear
  - src:
      layout: main-vertical
      panes:
        - source amana-env/bin/activate.fish && vs && cd src
  - data:
      layout: main-vertical
      panes:
        -  source amana-env/bin/activate.fish && vs && cd data
