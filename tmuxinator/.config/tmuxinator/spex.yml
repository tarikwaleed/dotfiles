name: spex
root: ~/repos/spex/

windows:
  - local:
      panes:
        - cd spex-django-v2 && vim
        - cd spex-django-v2 
        - cd spex-django-v2 
        - cd spex-django-v2 
  - droplet:
      panes:
        - ssh root@(cat ~/secrets/creds/spex-droplet-ip)
        - ssh root@(cat ~/secrets/creds/spex-droplet-ip)
        - ssh root@(cat ~/secrets/creds/spex-droplet-ip)
        - ssh root@(cat ~/secrets/creds/spex-droplet-ip)
