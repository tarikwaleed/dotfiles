name: spex
root: ~/repos/spex/daftra-onecup-glue

windows:
  - console:
      layout: even-vertical
      panes:
        - source .venv/bin/activate.fish 
        - source .venv/bin/activate.fish && dj2sh
        - ssh root@(cat ~/secrets/creds/my-vps-ip)
        - ssh root@(cat ~/secrets/creds/my-vps-ip)
