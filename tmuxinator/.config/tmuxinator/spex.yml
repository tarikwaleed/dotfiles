name: spex
root: ~/repos/spex/

windows:
  - editor:
      panes:
        - cd spex-django-v2 && source ./app/.venv/bin/activate.fish && vim
  - console:
      layout: even-vertical
      panes:
        - cd spex-django-v2 && source ./app/.venv/bin/activate.fish 
        - cd spex-django-v2 && source ./app/.venv/bin/activate.fish && dj2sh
  - droplet:
      panes:
        - ssh tarik@(cat ~/secrets/creds/spex-droplet-ip)
        - ssh tarik@(cat ~/secrets/creds/spex-droplet-ip)
        - ssh tarik@(cat ~/secrets/creds/spex-droplet-ip)
        - ssh tarik@(cat ~/secrets/creds/spex-droplet-ip)
