# /home/tarik/.config/tmuxinator/digihaven.yml

name: tse
root: ~/repos/tse

windows:
  - local:
      layout: even-vertical
      panes:
        - cd ads-dashboard/tse-ads-dashboard-backend && source app/.venv/bin/activate.fish && pycharm-professional .
  - server:
      layout: even-vertical
      panes:
        - ssh -X root@(cat ~/secrets/creds/my-vps-ip)
        - ssh -X root@(cat ~/secrets/creds/my-vps-ip)
        - ssh -X root@(cat ~/secrets/creds/my-vps-ip)
        - ssh -X root@(cat ~/secrets/creds/my-vps-ip)

