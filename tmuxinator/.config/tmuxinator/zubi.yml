name: zubi
root: ~/work/zubi

windows:
  - next:
      panes:
        - cd zubi-law-next
        - cd zubi-law-next
        - cd zubi-law-next
        - cd zubi-law-next
  - django:
      panes:
        - cd zubi-law-django
        - cd zubi-law-django
        - cd zubi-law-django
        - cd zubi-law-django
  - server:
      panes:
        - ssh root@(cat ~/secrets/creds/zubi-backend-droplet-ip)
        - ssh root@(cat ~/secrets/creds/zubi-backend-droplet-ip)
        - ssh root@(cat ~/secrets/creds/zubi-backend-droplet-ip)
        - ssh root@(cat ~/secrets/creds/zubi-backend-droplet-ip)
