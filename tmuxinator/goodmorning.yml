# /home/takizee/.config/tmuxinator/goodmorning.yml

name: goodmorning
root: ~/
windows:
  - Terminal:
    - clear
  - Dotfiles:
      panes:
        - clear && vim ~/.vimrc 
        - clear && vim /mnt/c/Users/takizee/.ideavimrc
        - clear && vim ~/.config/fish/config.fish
        - clear && vim ~/.tmux.conf

